Write code in Java selenium to test below scenario: 
IDENTIFICATION DIVISION.
PROGRAM-ID. SampleProgram.
AUTHOR. Your Name.
DATE-WRITTEN. Todays-Date.

DATA DIVISION.
WORKING-STORAGE SECTION.
01 COUNTER PIC 9(3) VALUE 1.
01 TOTAL-SUM PIC 9(5) VALUE 0.

01 NUMBERS OCCURS 10 TIMES PIC 9(3).
01 RESULT-MESSAGE PIC X(50).

PROCEDURE DIVISION.
    PERFORM INITIALIZE-DATA.



PERFORM VARYING COUNTER FROM 1 BY 1 UNTIL COUNTER > 10
    COMPUTE TOTAL-SUM = TOTAL-SUM + NUMBERS(COUNTER)
END-PERFORM.

PERFORM DISPLAY-RESULTS.

STOP RUN.
INITIALIZE-DATA.
    MOVE 10 TO NUMBERS(1).
    MOVE 20 TO NUMBERS(2).
    MOVE 30 TO NUMBERS(3).
    MOVE 40 TO NUMBERS(4).
    MOVE 50 TO NUMBERS(5).
    MOVE 60 TO NUMBERS(6).
    MOVE 70 TO NUMBERS(7).
    MOVE 80 TO NUMBERS(8).
    MOVE 90 TO NUMBERS(9).
    MOVE 100 TO NUMBERS(10).

DISPLAY-RESULTS.
    MOVE 'The sum of the numbers is: ' TO RESULT-MESSAGE.
    DISPLAY RESULT-MESSAGE TOTAL-SUM.